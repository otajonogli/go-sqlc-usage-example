version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "database"
        out: "."
        emit_json_tags: true
        emit_empty_slices: true
        emit_exact_table_names: false
